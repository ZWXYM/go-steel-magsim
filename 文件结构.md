YUCE/（项目根目录）
│
├── app.py                          ← Flask 后端主程序（API 服务器）
├── 1.py                            ← 织构取向生成核心模块（ODF模型）
├── generate_individual_scripts.py  ← MX3 仿真脚本生成器
├── generate_parallel_batch.py      ← 批处理脚本生成器（BAT/PS1/SH）
├── see.py                          ← 仿真结果分析模块（磁滞回线提取）
│
├── templates/
│   └── index.html                  ← 前端单页应用（SPA）
│
│── ─ ─ 以下目录由程序运行时自动创建 ─ ─ ─
│
├── preinput/                       ← 织构生成输出目录（1.py → app.py）
│   ├── grain_orientations_ODF_fGoss0.70_theta15_hw10_N1000.txt
│   │                               ↑ 单一生成文件（直接存放）
│   │
│   └── batch_lhs_20260320_143022/ ← 批量生成子文件夹（每批次一个）
│       ├── grain_orientations_ODF_001_fGoss0.42_theta5_hw8_N100.txt
│       ├── grain_orientations_ODF_002_fGoss0.55_theta12_hw11_N100.txt
│       └── ...（n_samples 个文件）
│
├── input/                          ← MX3 脚本生成的输入目录
│   ├── grain_orientations_ODF_fGoss0.70_theta15_hw10_N1000.txt
│   │                               ↑ 从 preinput 手动移入的单个文件
│   │
│   └── batch_lhs_20260320_143022/ ← 从 preinput 整批移入的子文件夹
│       ├── grain_orientations_ODF_001_fGoss0.42_theta5_hw8_N100.txt
│       └── ...
│
├── grain_scripts/                  ← MX3 仿真脚本输出目录
│   └── F70_T15_N1000/              ← 每个配置一个子目录（短名称）
│       ├── simulation_parameters_template.txt   ← 仿真参数说明文件
│       ├── angle_000/              ← 每个仿真角度一个子目录（single模式）
│       │   ├── grain_001.mx3
│       │   ├── grain_002.mx3
│       │   └── ...（n_grains 个文件）
│       ├── angle_030/
│       │   ├── grain_001.mx3
│       │   └── ...
│       └── all_angles_combined/    ← complex 模式下的输出目录
│           ├── grain_001_multiangle.mx3
│           └── ...
│
├── output/                         ← MuMax3 仿真结果存放目录
│   └── run_F70_T15_N1000_angles_0_30_45_20260320_150000/
│       │                           ↑ 单配置运行目录（批处理脚本命名）
│       ├── simulation_parameters_template.txt
│       ├── failed_list.txt         ← 仅在有失败任务时生成
│       ├── angle_000/
│       │   ├── grain_001.txt       ← MuMax3 输出的 table.txt（已重命名）
│       │   ├── grain_002.txt
│       │   └── ...
│       └── angle_030/
│           └── ...
│   └── run_multi_configs_20260320_160000/
│       │                           ↑ 多配置运行目录
│       ├── F70_T15_N1000/
│       │   ├── angle_000/
│       │   └── ...
│       └── F44_T5_N100/
│           └── ...
│
├── run_F70_T15_N1000_angles_0_30_45.bat   ← Windows 批处理脚本
├── run_F70_T15_N1000_angles_0_30_45.ps1   ← PowerShell 脚本
├── run_F70_T15_N1000_angles_0_30_45.sh    ← Linux Bash 脚本
│   ↑ 以上三类批处理文件直接生成在根目录
│
└── pole_figures_ODF_*.png          ← 极图（plot_odf=True 时生成，存于对应批次目录）