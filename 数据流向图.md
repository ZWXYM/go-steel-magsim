1.py
 │  单一生成 → preinput/grain_orientations_ODF_*.txt
 │  批量生成 → preinput/batch_{method}_{timestamp}/*.txt
 ↓
input/（手动从 preinput 移入，或批次文件夹整体移入）
 ↓
generate_individual_scripts.py
 │  读取 input/ 中的 .txt 文件（支持单个文件 & 批次子目录）
 │  single 模式 → grain_scripts/{配置名}/angle_{角度}/grain_*.mx3
 │  complex 模式 → grain_scripts/{配置名}/all_angles_combined/grain_*_multiangle.mx3
 ↓
generate_parallel_batch.py
 │  扫描 grain_scripts/ → 生成 run_*.bat / .ps1 / .sh
 ↓
MuMax3 仿真（批处理脚本执行）
 │  读取 grain_scripts/ 中的 .mx3 文件
 │  输出 table.txt → 移动到 output/run_{...}/angle_{角度}/grain_*.txt
 ↓
see.py / 结果分析页面
   读取 output/ 中的 grain_*.txt → 提取 Ms, Mr, Hc, μ_max, 磁滞损耗等